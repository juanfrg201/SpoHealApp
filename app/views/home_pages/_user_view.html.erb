<div class="container conteiner-margin">
  <div class="hero-body">
    <div class="container">
      <!-- Card con tabla -->
      <div class="card">
        <header class="card-header">
          <p class="card-header-title">
            Días Activos
          </p>
        </header>
        <div class="card-content">
          <div class="content mt-0">
    
            <%= render 'table_info' %>
          </div>
        </div>
      </div>

      <!-- Agregamos una separación entre las tarjetas -->
      <div class="mt-4"></div>

      <!-- Card con recomendaciones -->
      <div class="content has-text-centered">
        <p class=" has-text-success">Tips Nutricionales</p>
      </div>
      <div class="card has-shadow-8">
        
        <% @nutricions.each do |nutricion| %>
          <%= link_to nutricion_tip_path(nutricion), class: "content-container has-text-black"  do %>
            <div class="custom-section-container first-custom-section">
              <div class="left-section">
                <div class="image-container">
                  <img src="<%= asset_path('corredor.png') %>" alt="Logo corredor" class="image">
                </div>
              </div>
              <div class="right-section">
                <div class="">
                  <strong class="section-text" style="display: block; text-align: center;">
                    <%= nutricion.name %>
                  </strong>
                </div>
                <div class="">
                  <p class="section-text"><%= nutricion.benefits %></p>
                </div>
              </div>
            </div>
          <% end %>
        <% end %>

        
      </div>
    </div>
  </div>
</div>