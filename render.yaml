databases:
  - name: SpoHeal
    ipAllowList: []

services:
  - type: web
    name: SpoHeal
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec rails s; bundle exec sidekiq -C config/sidekiq.yml"
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: SpoHeal
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
  - type: redis
    name: redis
    ipAllowList: []
    plan: free
    maxmemoryPolicy: noeviction
  - type: worker
    name: sidekiq-worker
    runtime: ruby
    region: ohio
    buildCommand: bundle install
    startCommand: bundle exec sidekiq
    envVars:
      - key: redis://red-cmn9c6i1hbls73ckho9g:6379
        fromService:
          type: redis
          name: sidekiq-redis
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false